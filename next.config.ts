import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "tzahile.co.il",
      "images.unsplash.com",
      "tecdn.b-cdn.net",
      "plus.unsplash.com",
      "recipe-cpsa.com",
    ],
  },
};

export default nextConfig;
